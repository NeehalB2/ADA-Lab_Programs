int gcd(int m, int n)
{
  while (m != n) {
     if (m > n)
         m = m - n;
     else
         n = n - m;
  }
  return m;
}
int main()
{
    int m = 144, n = 47;
    printf("%d\n", gcd(m, n));
    return 0;
}
  
  
