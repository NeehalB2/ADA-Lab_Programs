#include <stdio.h>
int r_gcd(int m, int n);
int main()
{
 int num1, num2, gcd;
 printf("Enter any two numbers to find GCD: );
 return 0;
 }
 int r_gcd(int m, int n)
 {
  if(n==0)
   return m;
  else
   return r_gcd(n, m%n);
}
